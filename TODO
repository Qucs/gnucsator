- extend the component library, see STATUS
- ac dump qucs format, possibly include noise
- add the missing simulations or command wrappers
- Eqn: access probes
- complex, vector, matrix parameters
- more functions, functions with >1 args
- get rid of wrapper gnucsator.sh
- write directly to output file. and only output
- fix logic. multiport and process

- problems in qucs to be addressed:
 - dataset format
 - (unnecessary) syntax irregularities
 - probe handling
 - ground node handling
